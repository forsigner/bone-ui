{
  "name": "bone-ui-root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "example"
    ]
  },
  "scripts": {
    "dev": "lerna run --parallel --stream start",
    "build": "lerna run --stream build",
    "test": "lerna run --parallel --stream test",
    "coveralls": "jest --coverage && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage",
    "publish": "lerna publish --force-publish=*",
    "format": "prettier --write ."
  },
  "eslintConfig": {
    "extends": "tie-app"
  },
  "author": "@forsigner",
  "devDependencies": {
    "@types/react": "^17.0.8",
    "@types/prettier": "^2.2.3",
    "@types/react-dom": "^17.0.5",
    "coveralls": "^3.1.0",
    "eslint-config-tie-app": "0.0.5",
    "husky": "^6.0.0",
    "lerna": "^3.22.1",
    "prettier": "^2.3.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "tsdx": "^0.14.1",
    "tslib": "^2.2.0",
    "typescript": "^4.3.2"
  }
}
